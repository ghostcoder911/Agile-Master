# Agile Master

This is an agent-assisted agile workspace. Prefer honest board state over ceremony.

When changing behavior, keep:

- One active sprint
- Daily logs as first-class data
- Coach actions that mutate the board (assign, move, follow-up sweep)
- Contribution scores framed as coaching, not HR ranking
